// TomTom UX Library — Claude Code context file
// Generated by the TomTom Developer Docs portal.
// Drop this file into your project root as CLAUDE.md, or paste it as
// context into any AI coding assistant.

export const UX_LIBRARY_CLAUDE_CONTEXT = `# TomTom UX Library — Claude Code Context

> This file gives an AI coding assistant everything it needs to help you
> integrate the TomTom UX Library. Keep it in your project root as CLAUDE.md.

---

## What this library is

The TomTom UX Library is an Android Jetpack Compose UI component library that sits
**on top of** the Maps & Navigation SDK. It provides pre-built, OEM-customisable
navigation UI components — instruction panels, ETA displays, route bars, search UIs,
and EV charging panels — so you can build a polished navigation HMI without designing
from scratch.

**Important:** The UX Library is NOT a replacement for the NavSDK. It depends on the
NavSDK and simply provides a UI layer above it.

---

## Requirements

- Minimum Android API level: **26** (Android 8.0 Oreo)
- Language: **Kotlin**
- UI: **Jetpack Compose only** — there are no XML Views equivalents
- Build system: **Gradle**

---

## Gradle Setup

\`\`\`groovy
// settings.gradle
repositories {
  maven { url 'https://repositories.tomtom.com/artifactory/maven' }
  google()
  mavenCentral()
}
\`\`\`

\`\`\`groovy
// app/build.gradle
def uxLibraryVersion = "1.x.x" // replace with latest version

dependencies {
  // UX Library modules
  implementation "com.tomtom.ux:navigation-ux:$uxLibraryVersion"
  implementation "com.tomtom.ux:map-ux:$uxLibraryVersion"
  implementation "com.tomtom.ux:search-ux:$uxLibraryVersion"
  implementation "com.tomtom.ux:ev-ux:$uxLibraryVersion"

  // NavSDK is also required (UX Library depends on it)
  implementation "com.tomtom.sdk:maps-display:$navSdkVersion"
  implementation "com.tomtom.sdk:navigation-ui:$navSdkVersion"
}
\`\`\`

---

## API Key Setup

Same pattern as the NavSDK — add to \`AndroidManifest.xml\`:

\`\`\`xml
<application ...>
  <meta-data
    android:name="com.tomtom.sdk.maps.ApiKey"
    android:value="YOUR_API_KEY" />
</application>
\`\`\`

---

## Design Token System

The UX Library uses a token-based design system. All UX Library composables must be
wrapped in \`TomTomTheme\` to receive the correct visual styling.

\`\`\`kotlin
import com.tomtom.ux.theme.TomTomTheme
import com.tomtom.ux.theme.TomTomColors
import com.tomtom.ux.theme.TomTomTypography
import com.tomtom.ux.theme.TomTomShapes

TomTomTheme(
  colors = TomTomColors(
    primary = Color(0xFFE2001A),        // TomTom red
    onPrimary = Color.White,
    surface = Color(0xFF1A1A2E),
    accent = Color(0xFF58A6FF),
    // ... other token overrides
  ),
  typography = TomTomTypography(/* overrides */),
  shapes = TomTomShapes(/* overrides */),
) {
  // All UX Library composables go here
  MyNavigationScreen()
}
\`\`\`

### Token Categories

| Category   | Description | Scale / Notes |
|------------|-------------|---------------|
| Colors     | Primary, surface, accent, semantic (info/warn/danger/success) | Full palette |
| Typography | Heading, body, caption, monospace font stacks | Size scale |
| Shapes     | Corner radius for panels, cards, chips | 4dp base |
| Spacing    | Margin and padding scale | 4dp base grid |
| Elevation  | Shadow levels for overlapping panels | 0–5 |

---

## Key Components

### NavigationInstructionPanel

Displays the current maneuver instruction and distance to the next turn.

\`\`\`kotlin
import com.tomtom.ux.navigation.NavigationInstructionPanel

// Driven by TomTomNavigation progress updates
navigation.addProgressUpdatedListener { progress ->
  NavigationInstructionPanel(
    instruction = progress.currentInstruction,
    distanceToManeuver = progress.distanceToManeuver
  )
}
\`\`\`

### ETAPanel

Shows arrival time, remaining distance, and remaining travel time.

\`\`\`kotlin
import com.tomtom.ux.navigation.ETAPanel

ETAPanel(
  arrivalTime = progress.arrivalTime,
  remainingDistance = progress.remainingDistance,
  remainingTime = progress.remainingTime
)
\`\`\`

### MapDisplayComposable (UX layer)

The UX Library's map composable — wraps the NavSDK map with UX-layer defaults.

\`\`\`kotlin
import com.tomtom.ux.map.MapDisplayComposable

MapDisplayComposable(
  modifier = Modifier.fillMaxSize(),
  mapOptions = MapOptions(),
  onMapReady = { tomTomMap ->
    // Configure the map
  }
)
\`\`\`

### SearchBar

A pre-built search input with result suggestions.

\`\`\`kotlin
import com.tomtom.ux.search.SearchBar

SearchBar(
  placeholder = "Search for a place",
  onQueryChange = { query ->
    // Trigger search
  },
  onResultSelected = { result ->
    // Handle selected POI
  }
)
\`\`\`

### EVChargingPanel

Displays EV battery level, charging stop information, and remaining range.

\`\`\`kotlin
import com.tomtom.ux.ev.EVChargingPanel

EVChargingPanel(
  batteryLevel = 0.42f,          // 0.0 to 1.0
  chargingStops = listOf(stop1, stop2),
  remainingRange = 87_000        // metres
)
\`\`\`

### RouteBar

Summarises the active route progress at the bottom of the screen.

\`\`\`kotlin
import com.tomtom.ux.navigation.RouteBar

RouteBar(routeProgress = progress)
\`\`\`

---

## Map Customisation

\`\`\`kotlin
import com.tomtom.ux.map.UXLibraryStyles

// Built-in UX Library map styles
tomTomMap.loadStyle(UXLibraryStyles.browseDay)
tomTomMap.loadStyle(UXLibraryStyles.driveDay)
tomTomMap.loadStyle(UXLibraryStyles.driveNight)

// Custom style URL
tomTomMap.loadStyle(StyleDescriptor(Uri.parse("https://your-custom-style.json")))

// Traffic
tomTomMap.setTrafficFlowEnabled(true)
tomTomMap.setTrafficIncidentsEnabled(true)
\`\`\`

---

## App Customisation

Override the full design token set via \`TomTomTheme\`:

\`\`\`kotlin
TomTomTheme(
  colors = TomTomColors(
    primary = Color(0xFF003087),     // OEM brand colour
    onPrimary = Color.White,
    surface = Color(0xFF1C1C1E),
    accent = Color(0xFFFFA500),
  ),
  icons = TomTomIcons(
    turnLeft = R.drawable.oem_turn_left,
    turnRight = R.drawable.oem_turn_right,
    // ... other icon overrides
  )
) {
  NavigationInstructionPanel(instruction = instruction, distanceToManeuver = distance)
}
\`\`\`

Style tokens set at the component level override theme tokens for that instance only.

---

## Domain Overview

| Domain               | Components / Features |
|----------------------|-----------------------|
| Map Customisation    | UXLibraryStyles, traffic toggles, custom style URL |
| App Customisation    | TomTomTheme, TomTomColors, TomTomTypography, TomTomShapes, TomTomIcons |
| Navigation UI        | NavigationInstructionPanel, ETAPanel, RouteBar |
| Search               | SearchBar, result list |
| EV & Charging        | EVChargingPanel, charging stop cards |
| Vehicle Integration  | Instrument cluster (AAOS), HUD — requires Android Automotive OS |

---

## Architecture

\`\`\`
Your App
  └── TomTomTheme
        └── UX Library Components (com.tomtom.ux:*)
              └── Maps & Navigation SDK (com.tomtom.sdk:*)
                    └── TomTom REST APIs
\`\`\`

---

## Localisation

- Chinese localisation (Simplified + Traditional) supported via Android resource files
- RTL layout support included for Arabic and Hebrew markets
- Override string resources in your app's \`res/values-zh/strings.xml\` etc.

---

## Common gotchas

- **\`TomTomTheme\` must wrap all UX Library composables** — without it, components render with incorrect colours, typography, and spacing.
- **Compose only — no XML Views** — there are no \`View\`-based equivalents. If you need XML Views, use the NavSDK directly.
- **API key still goes in \`AndroidManifest.xml\`** — the UX Library uses the same API key mechanism as the NavSDK.
- **Style override at component level** — passing \`colors\` directly to a component (if the API allows) overrides the parent \`TomTomTheme\` tokens for that instance only, not globally.
- **Vehicle Integration (Cluster / HUD) requires AAOS** — Android Automotive OS components will not work on a phone or tablet.
- **EVChargingPanel \`batteryLevel\` range** — expects \`Float\` between 0.0 and 1.0. Passing a percentage (e.g. 42f instead of 0.42f) will cause display bugs.

---

## Useful links

- UX Library Reference: https://developer.tomtom.com/android/ux-library/documentation
- Pricing: https://developer.tomtom.com/pricing
- SDK Explorer: https://premalmistry-tomtom.github.io/ux-library-docs/
`;

export function downloadUXLibraryContext() {
  const blob = new Blob([UX_LIBRARY_CLAUDE_CONTEXT], { type: 'text/markdown;charset=utf-8' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'tomtom-ux-library.md';
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}
